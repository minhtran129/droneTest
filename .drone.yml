build:
  image: fdplugins/remote-build:0.6.2
  commands:
    - ruby ~/remote_build.rb
    - framgia-ci report
    - framgia-ci finish

  environment:
    # config remote build machine and user
    - USER_NAME=test04
    - KEY_CHAIN=login
    - KEY_CHAIN_PASS=Aa@123456
    - REMOTE_IP=10.0.1.119
    # config beta build. When push event to branch
    - BUILD_BRANCH=master
    - BUILD_LANE=beta
# deploy:
#   ssh-mac:
#     image: fdplugins/remote-build:0.3.1
#     host: 192.168.5.21
#     user: tran.vuong.minh
#     keychain: Minh1209
#     swiftlint: true
#     oclint: false
#     fastlane: beta
